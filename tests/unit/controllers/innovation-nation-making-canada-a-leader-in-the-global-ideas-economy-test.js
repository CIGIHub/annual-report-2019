import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Controller | innovation-nation-making-canada-a-leader-in-the-global-ideas-economy', function(hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    const controller = this.owner.lookup('controller:innovation-nation-making-canada-a-leader-in-the-global-ideas-economy');
    assert.ok(controller);
  });
});
